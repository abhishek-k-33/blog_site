<%- include("partials/header.ejs") %>

<article class="single-post">
    <!-- It grabs the title, author and date from the post object from our server and dynamically prints them into html. -->
    <header class="post-header">
        <h2><%= post.title %></h2>
        <div class="post-meta">
            <span class="author">By <%= post.author %></span> • <span class="date"><%= post.date %></span>
            <% if (post.readingTime) { %>
                • <span class="reading-time"><%= post.readingTime %> min read</span>
            <% } %>
        </div>
    </header>
    
    <div class="post-content">
        <% post.content.split('\n').forEach(paragraph => { %>
            <% if (paragraph.trim()) { %>
                <p><%= paragraph %></p>
            <% } %>
        <% }) %>
    </div>
    
    <div class="post-actions">
        <a href="/edit/<%= post.id %>" class="btn btn-secondary">Edit Post</a>
        <form action="/delete/<%= post.id %>" method="POST" style="display: inline;">
            <button type="submit" class="btn btn-danger" onclick="return confirm('Are you sure you want to delete this post?');">Delete Post</button>
        </form>
    </div>
</article>

<div class="back-link">
    <a href="/">← Back to Home</a>
</div>

<%- include("partials/footer.ejs") %>
