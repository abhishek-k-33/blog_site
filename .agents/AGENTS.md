# Workspace Behavioral Rules

- **No Commits**: Do NOT make git commits (`git commit`) unless the user explicitly asks you to commit.
- **No Git Push**: Do NOT push commits to GitHub (`git push`) unless the user explicitly commands you to push.
