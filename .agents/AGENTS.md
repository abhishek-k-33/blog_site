# Workspace Behavioral Rules

- **Granular Local Commits**: Make frequent, granular local git commits after completing any small, logical step or fixing a bug.
- **No Git Push**: NEVER push commits to GitHub (`git push`) unless the user explicitly commands you to push.
